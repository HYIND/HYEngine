layout(set = 1, binding = 0) uniform Camera { 
    mat4 projection;
    mat4 view;
    mat4 projView;
    mat4 invProjection;
    mat4 invView;
    mat4 invProjView;
    mat3 invTransView;
    vec3 position;
    vec3 direction;
    vec3 directionUp;
    vec3 directionRight;
    float nearPlane;
    float farPlane;
    float fov;
} camera;

layout(set = 1, binding = 1) uniform PrevCamera { 
    mat4 projection;
    mat4 view;
    mat4 projView;
    mat4 invProjection;
    mat4 invView;
    mat4 invProjView;
    mat3 invTransView;
    vec3 position;
    vec3 direction;
    vec3 directionUp;
    vec3 directionRight;
    float nearPlane;
    float farPlane;
    float fov;
} prevcamera;